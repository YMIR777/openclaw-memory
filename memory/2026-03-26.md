# 2026-03-26 日记

## 📱 OpenHarmony 开发：bmilxf202404020218 项目

### 项目路径
- `/Users/Ymir/Documents/trae_projects/OpenHarmony/bmilxf202404020218`

### 今日完成
- 创建了完整的 OpenHarmony 开发 skill：`~/.openclaw/workspace/skills/openharmony-dev/SKILL.md`
- 深度研究了 DevEco Studio 和 OpenHarmony 构建系统
- 基于 bmi_calculator 项目的成功经验，建立了完整的项目创建规范

### 关键经验（已沉淀到 skill）
1. **hvigor 缓存路径问题是 ENOENT 错误的根因**
   - hvigor daemon 在 `~/.hvigor/project_caches/<hash>/workspace/node_modules/@ohos/` 下创建 symlink
   - 项目路径变化后 symlink 指向不存在的旧路径
   - 解决：`rm -rf ~/.hvigor/project_caches/<hash>/workspace` 或手动修正 symlink

2. **系统 API 不需要安装**：`@ohos/*` 是 SDK 内置的，不能写在 oh-package.json5 的 dependencies 里

3. **SDK 版本格式**：OpenHarmony 5.0 = `compileSdkVersion: 10`（整数），HarmonyOS 用字符串

4. **DevEco Studio GUI 才是主力**，hvigorw CLI 依赖环境变量，通常报错

5. **Canvas API**：`RenderingContextSettings(true)` + `lineCap: "round"`（字符串，不是枚举）

### 项目状态
- 项目结构已创建完毕（CC 写源码 + 我处理工具链）
- DevEco Studio 打开后 symlink 路径问题导致 hvigor 构建报错
- 已修正 symlink，等待验证

### 创建的 skill
- `~/.openclaw/workspace/skills/openharmony-dev/SKILL.md` — OpenHarmony 开发完整规范

## 🧠 知识沉淀
- LESSONS-核心经验.md 已更新 OpenHarmony 相关经验

## 🔧 image 工具 MiniMax VL 配置（重大突破）

### 问题背景
用户发来参考图 `/Users/Ymir/Downloads/任务.jpg`，image 工具连续报错无法读取。

### 根因分析
1. `OPENAI_API_KEY=ollama-local` → image 工具路由到 Ollama，但 Ollama 无视觉模型
2. `imageModel` 配置成 dict 格式时被 patch 展开，变成 `{"primary":"minimax/MiniMax-VL-01","fallbacks":[]}`
3. `models.providers.ollama` 存在 → `resolveImageFallbackCandidates` fallback 到 ollama → "No media-understanding provider registered for ollama"

### 解决方案
1. `config.patch` 添加 `MINIMAX_API_KEY` 环境变量（sk-cp-uYnOWm63FbNSp7AJbq5NbZUOBFSIcV94xwt0DAa_4JbUiqI1Tvk2wdseajM9pFSiGZZcanCO-FTXI4CaQyGeOOwFOSu-Da9ZoPbkyjUw8VWOHvpStrp0NIM）
2. `agents.defaults.imageModel.primary = "minimax/MiniMax-VL-01"`（字符串格式）
3. **删掉 `models.providers.ollama`** → 这样 fallback 链无法到达 ollama，直接走 minimax VL
4. Gateway 重启后 MiniMax VL 工作正常

### 关键代码路径
- `resolveImageFallbackCandidates` → `resolveImageModelConfigForTool` → `hasToolModelConfig` 判断是否使用配置的 imageModel
- `isMinimaxVlmProvider("minimax")` → 触发 MiniMax VL `/v1/coding_plan/vlm` 端点
- MiniMax Coding Plan key 的 `/v1/coding_plan/vlm` 端点支持 MiniMax-VL-01

### Ollama qwen2.5vl 探索（失败）
- qwen2.5vl:7b（6GB）：OOM，"model runner has unexpectedly stopped"
- qwen2.5vl:3b（ModelScope safetensors）：文本正常，vision 报同样错误
- **根因**：Ollama qwen2.5vl 需要融合的单个 GGUF 文件，safetensors 分片导入无法正确处理 vision 权重融合
- 结论：Ollama 本地 vision 模型需官方预融合 GGUF 格式，暂时不可用

### 最终配置
- **MiniMax VL**：正常工作，image 工具主力
- **Ollama**：仅保留 nomic-embed-text（文本嵌入）
- `models.providers.ollama` 已从配置中移除

## 🌐 下载优化
- Ollama CDN 在国内极慢（5KB/s），改用 ModelScope下载快 20MB/s
- `pip install --user` 被 macOS PEP 668 限制，用 `python3 -m venv ~/.ollama-venv` 绕过

## ⚠️ Ollama Vision 探索：最终失败
- **结论**：Ollama 本地 vision 模型暂不可行（融合 GGUF 架构限制）
- **清理**：qwen2.5vl:3b 已删除，相关 blobs（7GB）、mmproj（1.2GB）、temp 文件全部清理
- **最终方案**：MiniMax VL（image 工具）正常工作，作为图片识别主力

## 📐 UML 作业完成
- 参考图：`/Users/Ymir/Downloads/任务.jpg`（Sort 函数代码 + 程序控制流图）
- MiniMax VL 读取参考图后，绘制 `SortControlFlow.png`
- 路径：`/Users/Ymir/Documents/trae_projects/UML作业/SortControlFlow.png`

## 📅 今日事件
- GitHub token 更新认证完成
- skills-manager 推送 GitHub
- Token 优化配置应用（contextPruning、heartbeat.lightContext、thinkingDefault:high）
- PlantUML UML 作业完成（ATM 存款流程控制图已删除）
- MEMORY.md 拆分为 topics/ 目录
- Ollama qwen2.5vl:7b 因 OOM 失败，改为 3B
- image 工具配置打通，MiniMax VL 正常工作
