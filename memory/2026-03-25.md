# 2026-03-25 Daily Memory

## GitHub Auth 永久修复
- git credential helper 已永久设为 `env`：`git config --global credential.helper "env"`
- 以后所有 git push/pull 自动用 `GITHUB_TOKEN` env，不再弹登录
- 修复子会话：agent:main:subagent:47dea232-14dd-443e-bd50-311b1ca7ed2a

## skills-manager 状态
- 路径：`~/.claw/skills/skills-manager/`（有独立 git repo）
- remote 已设为 `https://github.com/Ymir777/skills-manager.git`（但 repo 尚未创建）
- 待完成：创建 GitHub repo + push
- 子会话 agent:main:subagent:2e0b8e28-c0c3-42e3-8f8e-e9fba5fcf029 在跑，尝试用 GITHUB_TOKEN 建 repo

## 两个 Skills 路径分工（已确认）
| 路径 | 用途 | git 追踪 |
|------|------|---------|
| `~/.claw/skills/` | 手动安装（clawdhub 等） | 无独立 git |
| `~/.claw/workspace/skills/` | workspace 自建 skill | workspace git |

## GITHUB_TOKEN 现状
- env 中有：`[GITHUB_TOKEN_REDACTED]`
- gh CLI 不读这个 token（用 keychain），所以 `gh repo create` 失败
- 解决方案：用 curl 直接调 GitHub REST API，或让用户手动建 repo

## MEMORY.md 拆分（2026-03-25）
- MEMORY.md 拆分为 memory/topics/ 目录
- 新结构：USER、TOOLS、CLAUDE-CODE、LESSONS、QMD、GATEWAY、ROUTING
- MEMORY.md 改为索引 + 最近事件

## GitHub 上传工作流（2026-03-25 沉淀）
- 文档：~/Documents/个人知识体系/Ai工作流/VibeCoding/08-GitHub上传工作流.md
- gh 存 keychain，git 读 env GITHUB_TOKEN
- credential.helper=env 后所有 git push 不再弹登录
- 新 token [GITHUB_TOKEN_REDACTED] 已写入 ~/.zshrc

## 子 Agent 模型路由（2026-03-25）
- AGENTS.md: 已添加 Sub-Agent Model Routing 章节
- openclaw.json: subagents.model 已设为 minimax/MiniMax-M2.7
- 路由框架预留，等第二个模型加入后自动启用
- Coding plan 按文本量计费，思考级别 high 不影响费用

## Token 优化配置（2026-03-25 应用）
- contextPruning: cache-ttl / 1h
- heartbeat.lightContext: true
- bootstrapMaxChars: 20000 / bootstrapTotalMaxChars: 30000
- thinkingDefault: high
- contextTokens: 256000
- memorySearch.query.maxResults: 8 / minScore: 0.3

## 用户偏好
- 小黒不喜欢过于谄媚的回答
- 遇到权限问题应该彻底解决，不要每次都让用户手动操作
