
## 📝 基本路径测试作业经验（Sort 函数 ex2.3-1）

### 作业内容
- Sort 函数基本路径测试法练习
- 学号：202404020218，姓名：罗翔夫
- 输出：Word 文档 + Excel 测试用例

### 作业文件路径
- `/Users/Ymir/Documents/trae_projects/ex2-3-1/`

### 踩坑记录

#### 1. matplotlib 中文字体问题
- **问题**：`/System/Library/Fonts/STHeiti Medium.ttc` 字体路径在 `FontProperties(fname=...)` 中可用，但 matplotlib 字体管理器找不到
- **根因**：Python 3.14 的 matplotlib 字体注册机制变化，`FontProperties` 实例方法和 `fontManager.addfont` 不兼容
- **最终方案**：换用 **Pillow (PIL)** 渲染图片，`ImageFont.truetype()` 对 TTC 字体支持更好
  - 成功路径：`ImageFont.truetype('/System/Library/Fonts/Hiragino Sans GB.ttc', size)`

#### 2. Pillow rounded_rectangle width 参数类型
- **问题**：`width=1.5`（float）报错 `TypeError: 'float' object cannot be interpreted as an integer`
- **解决**：所有 width 必须是整数，改 `width=1.5` → `width=2`

#### 3. 流程图循环回边穿过节点问题
- **问题**：紫色循环回边起始段和节点10左边框重合/接触
- **解决**：将回边最左侧 x 坐标从 80 改为 35（远左边缘），完全避开左侧节点

#### 4. break 路径冗余标签
- **问题**：节点7写了 `x=y+2; break;` 后，箭头又加了一个红色 `break` 标签，冗余
- **解决**：去掉红色 break 标签，节点7本身已表达清楚

### 成功方案总结

**图片生成最佳实践（Mac Python 3.14）：**
```python
from PIL import Image, ImageDraw, ImageFont
FONT_PATH = '/System/Library/Fonts/Hiragino Sans GB.ttc'
def F(size): return ImageFont.truetype(FONT_PATH, size)
```
- Pillow 对 Mac TTC 中文字体支持好
- `ImageDraw.Draw` 可直接画箭头、多边形、圆角矩形
- 生成 PNG 后用 `add_picture()` 插入 Word

**Word 文档生成：**
```python
from docx import Document
doc.add_picture(path, width=Inches(6))  # 居中用 paragraph alignment
```

**Excel 测试用例：**
```python
from openpyxl import Workbook
wb = Workbook()
ws = wb.active
ws['A1'] = '值'
```
